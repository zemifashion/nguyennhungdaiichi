<div class="button-container {{ isset($class) ? $class : '' }}">
    <button class="button button-primary button-large" type="submit" id="{{ isset($id) ? $id : '' }}">{{ _wpcc('Save Changes') }}</button>
</div>